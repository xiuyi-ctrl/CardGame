# 驯牌远征 · 敌人 AI 逻辑设计方案

## 一、设计原则

1. **可预测性**：玩家能通过观察学习敌人行为模式，而非纯随机
2. **策略深度**：敌人会根据战场状态做出合理决策
3. **难度递进**：不同幕/类型的敌人 AI 复杂度不同
4. **机制联动**：AI 会利用自身被动、技能组合和状态效果
5. **公平性**：AI 不会获得玩家无法获得的信息或能力

## 二、AI 决策优先级（总览）

```
┌─────────────────────────────────────────────────────────────────┐
│                    每回合 AI 决策流程                           │
├─────────────────────────────────────────────────────────────────┤
│  Step 1: 状态检查                                              │
│  └── 眩晕 → 跳过行动（输出日志）                                │
│  Step 2: 治疗判定（优先级最高）                                 │
│  └── 血量 < 阈值 且 有治疗技能 且 治疗次数未耗尽 → 治疗        │
│  Step 3: 特殊技能判定（次高优先级）                             │
│  └── 战吼/护盾/群体控制等策略性技能                             │
│  Step 4: 换位判定                                              │
│  └── 残血前排 + 健康后排 → 换位保命                            │
│  Step 5: 攻击技能选择                                          │
│  └── 根据目标优先级、技能类型、状态配合选择最佳攻击             │
│  Step 6: 默认行动                                              │
│  └── 无可用技能时 → 观望（skip）                               │
└─────────────────────────────────────────────────────────────────┘
```

## 三、分阶段实施计划

### 阶段一：核心决策（治疗 + 攻击目标 + 战吼/防御）

**改动范围**：`battle.ts` 的 `enemyAct` 函数，约 150 行
**影响**：战斗体验最直接的变化

#### 3.1 治疗判定（最高优先级）

##### 3.1.1 治疗触发条件

| 条件           | 阈值      | 说明                               |
| :------------- | :-------- | :--------------------------------- |
| 自身血量百分比 | < 45%     | 评分 65（必须治疗）               |
| 自身血量百分比 | 45% ~ 55% | 评分 35（概率治疗）               |
| 队友血量百分比 | < 25%     | 评分 60（治疗血量最低的队友）     |

##### 3.1.2 治疗目标选择

```
治疗目标优先级：
1. 自身（血量 < 45%）
2. 血量最低的队友（血量 < 25%）
3. 自身（血量 < 55% 且无更紧急目标）
```

##### 3.1.3 治疗技能选择

| 技能                            | 使用条件                              | 目标选择               |
| :------------------------------ | :------------------------------------ | :--------------------- |
| 愈光（回复 8）                  | 该技能可用次数大于0                   | 优先自己或最低血量队友 |
| 水浴（回复 5）                  | 该技能可用次数大于0                   | 优先自己或最低血量队友 |
| 潮汐领域（全体回复 4 + 水幕）   | 该技能可用次数大于0 且 ≥ 2 名队友受伤 | 全体友方               |
| 水波冲击（攻击前排 + 自身回复） | 作为攻击技能使用，非纯治疗            | 见攻击逻辑             |

##### 3.1.4 敌方治疗次数限制

- 每个敌人按照自己拥有的治疗技能以及相应的使用次数，释放技能。次数用尽后该生物无法再使用该治疗技能。
- 治疗次数耗尽后，AI **不再选择任何治疗技能**
- 此限制防止战斗被无限拖长

#### 3.2 增益技能（战吼）

| 条件                                             | 使用概率                      |
| :----------------------------------------------- | :---------------------------- |
| 未激活战吼状态 且 技能次数 > 0 且 当前回合数为 1 | 普通 battle 0.4、特殊模式 0.7 |
| 未激活战吼状态 且 技能次数 > 0 且 血量 > 40%     | 25%                           |
| 血量 < 40%（濒死反扑）                           | max(chance, 60%)              |

> 战吼（伤害 +2，2 回合）是敌方重要的增伤手段，应在战斗早期或濒死时使用。

#### 3.3 防御技能（坚盾 / 盾反）

| 条件                              | 使用概率        |
| :-------------------------------- | :-------------- |
| 自身血量 < 40% 且 无护盾          | **50%**         |
| 坚盾限 5 次，盾反限 5 次且冷却 1 回合 | —               |

> 坚盾给 5 护盾，盾反提供 3 护盾 + 反击状态，用于应对高爆发回合。

#### 3.4 攻击目标优先级

```
┌─────────────────────────────────────────────────────────────────┐
│                    敌方攻击目标优先级                          │
├─────────────────────────────────────────────────────────────────┤
│  Level 1: 被嘲讽来源（嘲讽强制）                               │
│  └── 单体/随机技能仅能攻击嘲讽来源；全体/自身/队友技能不受限    │
│                                                                 │
│  Level 2: 血量最低的目标（30% 血量以下，幕1 +8分 / 幕2-3 +12分） │
│  └── 优先收割残血单位                                          │
│                                                                 │
│  Level 3: 核心威胁（高输出/治疗单位）                          │
│  └── 根据玩家阵容动态识别                                      │
│                                                                 │
│  Level 4: 携带最多种类状态的目标                               │
│  └── 配合自身被动（如蟒影对中毒目标 +3 伤害）                  │
│                                                                 │
│  Level 5: 随机目标                                             │
│  └── 无明确优先级时随机选择                                    │
└─────────────────────────────────────────────────────────────────┘
```

##### 核心威胁识别逻辑

AI 会识别以下类型的玩家单位为高威胁目标：

| 威胁类型       | 识别条件                              | 优先级权重 |
| :------------- | :------------------------------------ | :--------- |
| **治疗者**     | 拥有治疗技能                          | 幕1 +6，幕2-3 +10 |
| **高输出**     | 基础速度 ≥ 6 或 被动 power/frenzy     | 幕1 +6，幕2-3 +10 |

#### 3.5 实现方案：评分制

将 `enemyAct` 从线性判断改为**候选行动评分制**：

```typescript
function enemyAct(b: BattleState, actor: Unit): BattleState {
  // 1. 眩晕检查
  // 2. 构建候选行动列表 { action, score }
  //    - 治疗行动（基于血量阈值 + 队友血量评分）
  //    - 增益行动（战吼/坚盾/嘲讽的条件匹配度评分）
  //    - 换位行动（血量 + 位置条件评分）
  //    - 攻击行动（目标优先级 × 技能适配度评分）
  // 3. 按评分排序，加随机扰动，取最高分行动执行
}
```

每个候选行动的评分由以下因素加权：
- **治疗分**：自身血量越低分越高；队友血量 < 30% 时加分
- **增益分**：首回合战吼加分；血量 < 40% 时坚盾加分；幽灵召唤（ghostly_summon）在所有召唤物死亡时必用（score 80）
- **攻击分**：目标优先级 × 技能伤害；状态配合额外加分；灵魂回响（soul_echo）按灵魂数加分：≥10 灵魂 +25，≥5 灵魂 +15，≥3 灵魂 +8
- **换位分**：前排残血 + 健康后排存在时加分

最后用 softmax 加权随机抽取：`weight = exp((score - maxScore) / 温度)`，高分 = 更高概率，低分仍可能被抽到。

---

### 阶段二：高级行为（换位 + 限次高伤 + 状态配合）

**改动范围**：新增辅助函数 + `enemyAct` 扩展，约 100 行
**前置条件**：阶段一完成

#### 4.1 换位判定

##### 4.1.1 换位触发条件

| 条件                                                 | 触发概率            | 说明 |
| :--------------------------------------------------- | :------------------ | :--- |
| 自身是**前排** 且 血量 < 35% 且 有**后排**血量 > 50% | **40%**（残血后退） |      |
| 自身是**前排** 且 血量 < 25% 且 有**后排**血量 > 50% | **65%**（紧急保命） |      |
| 自身是**前排** 且 被嘲讽状态 且 血量 < 30%           | **50%**             |      |

##### 4.1.2 换位实现

- 与**血量最高的后排**交换位置
- 换位**不消耗行动点**（敌方无 AP 概念，换位即占用其行动）
- 每只敌方每场战斗**最多换位 2 次**（防止无限换位拖时间）
- 需要在 `Unit` 上新增 `swapCount` 字段追踪换位次数

#### 4.2 限次高伤技能（焚身爆 / 淬毒噬咬 / 噬魂斩）

| 条件                                               | 评分加分（softmax 权重）|
| :------------------------------------------------- | :---------------------- |
| 自身血量 < 35%（濒死反扑）                         | +40（最高优先，与下方互斥） |
| 目标已处于中毒/灼烧状态（配合被动增伤）            | +30（次优先，与下方互斥）   |
| 自身血量 > 50%（健康状态限次使用）                  | +15（最低优先）       |
| 守卫节点且自身血量 < 50%                           | 额外 +20（守卫加成）       |

#### 4.3 状态配合逻辑

> **注**：以下配合以**隐式评分**实现（每回合独立决策，无跨回合序列化），非"先上毒再攻击"的固定流程。

##### 中毒配合（蟒影 / 蛇狩）

| 条件                            | AI 行为                               |
| :------------------------------ | :------------------------------------ |
| 自身有「对中毒目标伤害 +N」被动 | 对已中毒目标攻击评分 +15（venomPower）|
| 目标已中毒 ≥ 3 层               | 攻击评分额外加分（状态配合）          |

##### 灼烧配合（余烬焚身 / 灼焰）

| 条件                            | AI 行为                               |
| :------------------------------ | :------------------------------------ |
| 自身有「对灼烧目标伤害 +N」被动 | 对已灼烧目标攻击评分 +15（scorchPlus）|
| 目标已灼烧 ≥ 3 层               | 攻击评分额外加分（状态配合）          |

##### 反伤配合（荆棘之躯 / 硬甲）

| 条件                | AI 行为                              |
| :------------------ | :----------------------------------- |
| 自身是反伤型单位    | 嘲讽技能评分 +20（反伤配合加分）    |

##### 波光环配合（潮汐巨蟹）

| 条件                                         | AI 行为                                              |
| :------------------------------------------- | :--------------------------------------------------- |
| 自身血量 > 50% 且无 comboBoost 状态          | 55% 概率使用**波光环**（波光环仅对 hits>1 的连击技能生效） |
| 已有 comboBoost 状态                                    | 连击/水炮射击等多段技能概率大幅提升（softmax 权重加成，温度=12） |
| 使用 comboBoost 时目标为非连击技能（蟹钳重击） | 不消耗 comboBoost，buff 保留至下次连击技能使用       |

#### 4.4 实现方案

新增辅助函数：

```typescript
/** 评估某个玩家单位的威胁值（用于攻击目标选择） */
function evaluateThreat(target: Unit, actor: Unit, b: BattleState): number

/** 评估某个技能对特定目标的适配度（伤害 + 状态配合 + 次数等） */
function evaluateSkillFit(skill: SkillDef, actor: Unit, target: Unit, b: BattleState): number

/** 判断是否满足换位条件 */
function shouldSwap(actor: Unit, b: BattleState): boolean
```

---

### 阶段三：模式差异化（幕次 + 特殊模式）

**改动范围**：`BattleState` 扩展 + `enemyAct` 分支 + 节点类型传递
**前置条件**：阶段二完成

#### 5.1 各幕 AI 攻击倾向

| 幕次     | 攻击倾向                                              | 说明                 |
| :------- | :---------------------------------------------------- | :------------------- |
| **幕 1** | 随机扰动为主（随机加 `rng * 15` 分）                  | 新手友好，行为较简单 |
| **幕 2** | 最低血量 +5、核心威胁 +10、状态配合 +15               | 开始有策略性         |
| **幕 3** | 最低血量 +8、核心威胁 +12、状态配合 +18               | 高智能，针对性强     |

实现方式：在评分制中，根据 `b.act` 给各评分维度追加固定加分（幕2/3 有明确策略倾向，幕1 靠随机扰动保持多样性）。

#### 5.2 群体控制技能（群体嘲刺）

> **注**：嘲讽/群体嘲刺技能 `kind='attack'`，走攻击评分循环（非buff循环）。反伤型单位给嘲讽技能额外加分（+20），但无概率触发机制。

| 条件                                                | AI 行为 |
| :-------------------------------------------------- | :------ |
| 自身是反伤型单位（thorns/thornRoyal）且敌方前排 ≥ 2 | 嘲讽/群体嘲刺评分 +20 |

#### 5.3 特殊模式 AI 调整

##### 5.3.1 斗兽场（1v1）

| 调整项   | 说明                                  |
| :------- | :------------------------------------ |
| 攻击目标 | 唯一目标，无选择                      |
| 治疗判定 | 血量 < 45% 时治疗（比普通战斗更保守） |
| 战吼使用 | 首回合 **70%** 使用（如果有）         |
| 换位     | 禁用（无后排可换）                    |
| 限次技能 | 无限次技能特殊处理（残血目标 +5 评分）|

##### 5.3.2 车轮战

| 调整项     | 说明                                           |
| :--------- | :--------------------------------------------- |
| 攻击目标   | 优先攻击当前在场的玩家单位                     |
| 治疗判定   | 血量 < 35% 时治疗（比普通战斗更激进）          |
| 战吼使用   | 首回合 70% 使用                                |
| 换位       | 禁用（只有 1 只在场上）                        |
| 刚切入替补 | 当回合**不出手**（`acted=true`），符合文档规则 |
| 限次技能   | 无限次技能特殊处理（残血 +4、全体 +5）        |

##### 5.3.3 被侵蚀（腐化节点）

| 调整项   | 说明                                    |
| :------- | :-------------------------------------- |
| 攻击倾向 | 更加**激进**（评分 +3）                  |
| 治疗判定 | 血量 < 30% 才治疗（更少治疗，更多攻击） |
| 战吼使用 | 首回合 70% 使用                         |

##### 5.3.4 守卫 / 首领

| 调整项   | 说明                              |
| :------- | :-------------------------------- |
| 攻击倾向 | 最高智能，完全按核心威胁优先级    |
| 治疗判定 | 血量 < 55% 时治疗（阈值 0.55）    |
| 战吼使用 | 首回合 **70%** 使用               |
| 限次技能 | 血量 < 50% 后倾向使用（狂暴模式） |
| 换位     | 禁用（守卫/首领不换位）           |

#### 5.4 实现方案

- 在 `BattleState` 或 `BattleOptions` 中传递 `act`（当前幕）和 `nodeType`（节点类型）
- `enemyAct` 根据这些字段调整概率阈值和评分权重
- 可通过 `BattleOptions` 新增 `aiMode?: 'normal' | 'arena' | 'gauntlet' | 'corrupted' | 'boss'` 字段

---

## 四、AI 日志输出规范

AI 的每个决策应在战斗日志中输出，便于玩家理解和学习：

| 决策类型 | 日志格式                                           | 示例 |
| :------- | :------------------------------------------------- | :--- |
| 治疗     | `[敌方] 灼刃 使用 愈光 治疗 自身，回复 8 点生命`   | —    |
| 战吼     | `[敌方] 迅天 使用了 战吼，攻击力提升！`            | —    |
| 换位     | `[敌方] 棘尾 退到后排！`                           | —    |
| 攻击     | `[敌方] 灼天 对 迅迅 使用 火焰斩击，造成 8 点伤害` | —    |
| 嘲讽     | `[敌方] 棘尾 对 灼灼 施加 嘲讽`                    | —    |
| 观望     | `[敌方] 泡泡 无可用技能，在观望…`                  | —    |

> 注：当前 `useSkillInner` 已自动生成技能日志，AI 额外日志仅在特殊决策时输出（如换位、观望）。

---

## 五、AI 行为决策树完整版

```
┌─────────────────────────────────────────────────────────────────┐
│                    完整 AI 决策树                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  每个敌方单位行动时：                                          │
│                                                                 │
│  ① 是否眩晕？                                                  │
│     ├── 是 → 输出「被眩晕」日志 → 结束行动                    │
│     └── 否 → 进入 ②                                           │
│                                                                 │
│  ② 是否有治疗技能 且 治疗次数 > 0？                            │
│     ├── 是 → 检查血量阈值                                     │
│     │   ├── 自身血量 < 治疗阈值 → 治疗自己 → 结束行动         │
│     │   ├── 队友血量 < 治疗阈值-10% → 治疗队友 → 结束行动    │
│     │   └── 否 → 进入 ③                                      │
│     └── 否 → 进入 ③                                           │
│                                                                 │
│  ③ 是否有策略性技能（战吼/坚盾/孢子召唤/孢子防护）？           │
│     ├── 是 → 检查使用条件（概率判定）                         │
│     │   ├── 首回合 且 战吼可用 → 70% 概率使用战吼（特殊模式统一 70%）│
│     │   ├── 自身血量 < 40% 且 坚盾可用 → 50% 概率使用坚盾     │
│     │   ├── 孢子召唤有空位 → 死亡小怪 60%，空列 40% 概率使用   │
│     │   ├── 无sporeShield且血量<50% → 55% 概率使用孢子防护    │
│     │   └── 否 → 进入 ④                                      │
│     └── 否 → 进入 ④                                           │
│                                                                 │
│  ④ 是否满足换位条件？                                          │
│     ├── 是（前排残血 + 健康后排）→ 40~65% 概率换位            │
│     └── 否 → 进入 ⑤                                           │
│                                                                 │
│  ⑤ 选择攻击技能（评分制）                                      │
│     ├── 有嘲讽状态 → 目标锁定嘲讽来源                         │
│     ├── 有状态配合被动 → 优先攻击带状态目标                   │
│     ├── 有残血目标（< 30%）→ 优先攻击                         │
│     ├── 有核心威胁（治疗/高输出）→ 优先攻击                   │
│     └── 以上都无 → 随机攻击                                   │
│                                                                 │
│  ⑥ 执行攻击 → 结束行动                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 六、风险提醒

1. **模拟测试**：每阶段改动后必须跑 `simulation.test.ts` 确保 20 种子中至少 1 局通关、0 卡死
2. **难度平衡**：AI 变强可能导致模拟胜率下降，可能需要同步调低敌人属性或提高玩家收益
3. **回合时长**：评分制比随机选择稍慢，但单次评分 < 0.1ms，影响可忽略
4. **不改动日志系统**：当前 `useSkillInner` 已自动生成技能日志，无需额外改动
