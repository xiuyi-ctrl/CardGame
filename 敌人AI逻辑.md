# 驯牌远征 · 敌人 AI 逻辑设计方案

## 一、设计原则

1. **可预测性**：玩家能通过观察学习敌人行为模式，而非纯随机
2. **策略深度**：敌人会根据战场状态做出合理决策
3. **难度递进**：不同幕/类型的敌人 AI 复杂度不同
4. **机制联动**：AI 会利用自身被动、技能组合和状态效果
5. **公平性**：AI 不会获得玩家无法获得的信息或能力

## 二、AI 决策优先级（总览）

```
┌─────────────────────────────────────────────────────────────────┐
│                    每回合 AI 决策流程                           │
├─────────────────────────────────────────────────────────────────┤
│  Step 1: 状态检查                                              │
│  └── 眩晕 → 跳过行动（输出日志）                                │
│  Step 2: 治疗判定（优先级最高）                                 │
│  └── 血量 < 阈值 且 有治疗技能 且 治疗次数未耗尽 → 治疗        │
│  Step 3: 特殊技能判定（次高优先级）                             │
│  └── 战吼/护盾/群体控制等策略性技能                             │
│  Step 4: 换位判定                                              │
│  └── 残血前排 + 健康后排 → 换位保命                            │
│  Step 5: 攻击技能选择                                          │
│  └── 根据目标优先级、技能类型、状态配合选择最佳攻击             │
│  Step 6: 默认行动                                              │
│  └── 无可用技能时 → 观望（skip）                               │
└─────────────────────────────────────────────────────────────────┘
```

## 三、分阶段实施计划

### 阶段一：核心决策（治疗 + 攻击目标 + 战吼/防御）

**改动范围**：`battle.ts` 的 `enemyAct` 函数，约 150 行
**影响**：战斗体验最直接的变化

#### 3.1 治疗判定（最高优先级）

##### 3.1.1 治疗触发条件

| 条件           | 阈值      | 说明                               |
| :------------- | :-------- | :--------------------------------- |
| 自身血量百分比 | < 40%     | **必须治疗**，优先级最高           |
| 自身血量百分比 | 40% ~ 55% | 概率治疗（70% 概率）               |
| 队友血量百分比 | < 30%     | 治疗血量最低的队友（如有群体治疗） |
| 队友血量百分比 | < 20%     | **必须治疗**队友（如有单体治疗）   |

##### 3.1.2 治疗目标选择

```
治疗目标优先级：
1. 自身（血量 < 40%）
2. 血量最低的队友（血量 < 30%）
3. 自身（血量 < 55% 且无更紧急目标）
4. 随机队友（血量 < 50%）
```

##### 3.1.3 治疗技能选择

| 技能                            | 使用条件                              | 目标选择               |
| :------------------------------ | :------------------------------------ | :--------------------- |
| 愈光（回复 8）                  | 该技能可用次数大于0                   | 优先自己或最低血量队友 |
| 水浴（回复 5）                  | 该技能可用次数大于0                   | 优先自己或最低血量队友 |
| 潮汐领域（全体回复 4 + 水幕）   | 该技能可用次数大于0 且 ≥ 2 名队友受伤 | 全体友方               |
| 水波冲击（攻击前排 + 自身回复） | 作为攻击技能使用，非纯治疗            | 见攻击逻辑             |

##### 3.1.4 敌方治疗次数限制

- 每个敌人按照自己拥有的治疗技能以及相应的使用次数，释放技能。次数用尽后该生物无法再使用该治疗技能。
- 治疗次数耗尽后，AI **不再选择任何治疗技能**
- 此限制防止战斗被无限拖长

#### 3.2 增益技能（战吼）

| 条件                                             | 使用概率                      |
| :----------------------------------------------- | :---------------------------- |
| 未激活战吼状态 且 技能次数 > 0 且 当前回合数为 1 | **70%**（首回合大概率开战吼，特殊模式统一 70%） |
| 未激活战吼状态 且 技能次数 > 0 且 血量 > 40%     | 25%                           |
| 血量 < 40%（濒死反扑）                           | max(chance, 60%)              |

> 战吼（伤害 +2，2 回合）是敌方重要的增伤手段，应在战斗早期或濒死时使用。

#### 3.3 防御技能（坚盾 / 盾反）

| 条件                              | 使用概率        |
| :-------------------------------- | :-------------- |
| 自身血量 < 40% 且 无护盾          | **50%**         |
| 坚盾限 5 次，盾反限 5 次且冷却 1 回合 | —               |

> 坚盾给 5 护盾，盾反提供 3 护盾 + 反击状态，用于应对高爆发回合。

#### 3.4 攻击目标优先级

```
┌─────────────────────────────────────────────────────────────────┐
│                    敌方攻击目标优先级                          │
├─────────────────────────────────────────────────────────────────┤
│  Level 1: 被嘲讽来源（嘲讽强制）                               │
│  └── 单体/随机技能仅能攻击嘲讽来源；全体/自身/队友技能不受限    │
│                                                                 │
│  Level 2: 血量最低的目标（30% 血量以下）                       │
│  └── 优先收割残血单位                                          │
│                                                                 │
│  Level 3: 核心威胁（高输出/治疗单位）                          │
│  └── 根据玩家阵容动态识别                                      │
│                                                                 │
│  Level 4: 携带最多种类状态的目标                               │
│  └── 配合自身被动（如蟒影对中毒目标 +3 伤害）                  │
│                                                                 │
│  Level 5: 随机目标                                             │
│  └── 无明确优先级时随机选择                                    │
└─────────────────────────────────────────────────────────────────┘
```

##### 核心威胁识别逻辑

AI 会识别以下类型的玩家单位为高威胁目标：

| 威胁类型       | 识别条件                              | 优先级权重 |
| :------------- | :------------------------------------ | :--------- |
| **治疗者**     | 拥有治疗技能且已使用过治疗            | +2         |
| **高输出**     | 速度 ≥ 6 或 伤害修正 ≥ 2（战吼/被动） | +2         |
| **低血量脆皮** | 最大生命 ≤ 15 且 当前血量 > 50%       | +2         |
| **状态施加者** | 已对敌方施加灼烧/中毒 ≥ 2 层          | +1         |

#### 3.5 实现方案：评分制

将 `enemyAct` 从线性判断改为**候选行动评分制**：

```typescript
function enemyAct(b: BattleState, actor: Unit): BattleState {
  // 1. 眩晕检查
  // 2. 构建候选行动列表 { action, score }
  //    - 治疗行动（基于血量阈值 + 队友血量评分）
  //    - 增益行动（战吼/坚盾/嘲讽的条件匹配度评分）
  //    - 换位行动（血量 + 位置条件评分）
  //    - 攻击行动（目标优先级 × 技能适配度评分）
  // 3. 按评分排序，加随机扰动，取最高分行动执行
}
```

每个候选行动的评分由以下因素加权：
- **治疗分**：自身血量越低分越高；队友血量 < 30% 时加分
- **增益分**：首回合战吼加分；血量 < 40% 时坚盾加分
- **攻击分**：目标优先级 × 技能伤害；状态配合额外加分
- **换位分**：前排残血 + 健康后排存在时加分

最后用 softmax 加权随机抽取：`weight = exp((score - maxScore) / 温度)`，高分 = 更高概率，低分仍可能被抽到。

---

### 阶段二：高级行为（换位 + 限次高伤 + 状态配合）

**改动范围**：新增辅助函数 + `enemyAct` 扩展，约 100 行
**前置条件**：阶段一完成

#### 4.1 换位判定

##### 4.1.1 换位触发条件

| 条件                                                 | 触发概率            | 说明 |
| :--------------------------------------------------- | :------------------ | :--- |
| 自身是**前排** 且 血量 < 35% 且 有**后排**血量 > 50% | **40%**（残血后退） |      |
| 自身是**前排** 且 血量 < 25% 且 有**后排**血量 > 40% | **65%**（紧急保命） |      |
| 自身是**前排** 且 被嘲讽状态 且 血量 < 30%           | **50%**             |      |

##### 4.1.2 换位实现

- 与**血量最高的后排**交换位置
- 换位**不消耗行动点**（敌方无 AP 概念，换位即占用其行动）
- 每只敌方每场战斗**最多换位 2 次**（防止无限换位拖时间）
- 需要在 `Unit` 上新增 `swapCount` 字段追踪换位次数

#### 4.2 限次高伤技能（焚身爆 / 淬毒噬咬 / 噬魂斩）

| 条件                                               | 使用概率                |
| :------------------------------------------------- | :---------------------- |
| 敌方核心单位（高威胁）血量 > 50% 且 自身血量 > 50% | **60%**（优先打高威胁） |
| 自身血量 < 35%（濒死反扑）                         | **90%**（必须用）       |
| 目标已处于中毒/灼烧状态（配合被动增伤）            | **80%**（收益最大化）   |

#### 4.3 状态配合逻辑

##### 中毒配合（蟒影 / 蛇狩）

| 条件                            | AI 行为                               |
| :------------------------------ | :------------------------------------ |
| 自身有「对中毒目标伤害 +N」被动 | 优先用**毒刺/毒雾**给目标上毒，再攻击 |
| 目标已中毒 ≥ 3 层               | 优先使用**高伤技能**攻击该目标        |
| 场上无中毒目标                  | 优先使用**毒刺/毒雾**而非直接攻击     |

##### 灼烧配合（余烬焚身 / 灼焰）

| 条件                            | AI 行为                               |
| :------------------------------ | :------------------------------------ |
| 自身有「对灼烧目标伤害 +N」被动 | 优先用**火花/烈焰爆发**上灼烧，再攻击 |
| 目标已灼烧 ≥ 3 层               | 优先使用**高伤技能**攻击该目标        |
| 场上无灼烧目标                  | 优先使用**火花/烈焰爆发**上灼烧       |

##### 反伤配合（荆棘之躯 / 硬甲）

| 条件                | AI 行为                              |
| :------------------ | :----------------------------------- |
| 自身是反伤型单位    | 优先使用**嘲讽/挑衅**吸引攻击        |
| 已激活荆棘/硬甲被动 | 故意暴露在前排（不换位）吸引物理攻击 |
| 敌方有多段攻击单位  | 优先**嘲讽**该单位（让其中反伤）     |

##### 波光环配合（潮汐巨蟹）

| 条件                                         | AI 行为                                              |
| :------------------------------------------- | :--------------------------------------------------- |
| 自身血量 > 50% 且无 comboBoost 状态          | 55% 概率使用**波光环**（波光环仅对 hits>1 的连击技能生效） |
| 已有 comboBoost 状态                                    | 连击/水炮射击等多段技能概率大幅提升（softmax 权重加成，温度=12） |
| 使用 comboBoost 时目标为非连击技能（蟹钳重击） | 不消耗 comboBoost，buff 保留至下次连击技能使用       |

##### 治疗+坦克配合

| 条件                  | AI 行为                                         |
| :-------------------- | :---------------------------------------------- |
| 队伍中有治疗者 + 坦克 | 治疗者优先给坦克回血，坦克优先使用**坚盾/盾反** |
| 坦克血量 < 40%        | 治疗者**必须**治疗坦克（优先级高于治疗自己）    |
| 治疗者被集火          | 坦克使用**嘲讽**吸引火力保护治疗者              |

#### 4.4 实现方案

新增辅助函数：

```typescript
/** 评估某个玩家单位的威胁值（用于攻击目标选择） */
function evaluateThreat(target: Unit, actor: Unit, b: BattleState): number

/** 评估某个技能对特定目标的适配度（伤害 + 状态配合 + 次数等） */
function evaluateSkillFit(skill: SkillDef, actor: Unit, target: Unit, b: BattleState): number

/** 判断是否满足换位条件 */
function shouldSwap(actor: Unit, b: BattleState): boolean
```

---

### 阶段三：模式差异化（幕次 + 特殊模式）

**改动范围**：`BattleState` 扩展 + `enemyAct` 分支 + 节点类型传递
**前置条件**：阶段二完成

#### 5.1 各幕 AI 攻击倾向

| 幕次     | 攻击倾向                                              | 说明                 |
| :------- | :---------------------------------------------------- | :------------------- |
| **幕 1** | 偏向随机攻击（60%）+ 最低血量（40%）                  | 新手友好，行为较简单 |
| **幕 2** | 偏向最低血量（50%）+ 核心威胁（30%）+ 状态配合（20%） | 开始有策略性         |
| **幕 3** | 偏向核心威胁（40%）+ 状态配合（35%）+ 最低血量（25%） | 高智能，针对性强     |

实现方式：在评分制中，根据 `b.act` 调整各评分维度的权重系数。

#### 5.2 群体控制技能（群体嘲刺）

| 条件                                                | 使用概率 |
| :-------------------------------------------------- | :------- |
| 敌方前排 ≥ 2 只 且 技能次数 > 0 且 自身血量 > 40%   | **60%**  |
| 敌方前排 ≥ 2 只 且 技能次数 > 0 且 自身是坦克型单位 | **75%**  |

#### 5.3 特殊模式 AI 调整

##### 5.3.1 斗兽场（1v1）

| 调整项   | 说明                                  |
| :------- | :------------------------------------ |
| 攻击目标 | 唯一目标，无选择                      |
| 治疗判定 | 血量 < 45% 时治疗（比普通战斗更保守） |
| 战吼使用 | 首回合 **70%** 使用（如果有）         |
| 换位     | 禁用（无后排可换）                    |
| 限次技能 | 血量 < 40% 时 **100%** 使用           |

##### 5.3.2 车轮战

| 调整项     | 说明                                           |
| :--------- | :--------------------------------------------- |
| 攻击目标   | 优先攻击当前在场的玩家单位                     |
| 治疗判定   | 血量 < 35% 时治疗（比普通战斗更激进）          |
| 战吼使用   | 首回合 70% 使用                                |
| 换位       | 禁用（只有 1 只在场上）                        |
| 刚切入替补 | 当回合**不出手**（`acted=true`），符合文档规则 |
| 限次技能   | 倾向于留在后期波次使用（保存实力）             |

##### 5.3.3 被侵蚀（腐化节点）

| 调整项   | 说明                                    |
| :------- | :-------------------------------------- |
| 攻击倾向 | 更加**激进**（攻击频率 +15%）           |
| 治疗判定 | 血量 < 30% 才治疗（更少治疗，更多攻击） |
| 战吼使用 | 首回合 70% 使用                         |

##### 5.3.4 守卫 / 首领

| 调整项   | 说明                              |
| :------- | :-------------------------------- |
| 攻击倾向 | 最高智能，完全按核心威胁优先级    |
| 治疗判定 | 血量 < 45% 时治疗（保守）         |
| 战吼使用 | 首回合 **70%** 使用               |
| 限次技能 | 血量 < 50% 后倾向使用（狂暴模式） |
| 换位     | 禁用（守卫/首领不换位）           |

#### 5.4 实现方案

- 在 `BattleState` 或 `BattleOptions` 中传递 `act`（当前幕）和 `nodeType`（节点类型）
- `enemyAct` 根据这些字段调整概率阈值和评分权重
- 可通过 `BattleOptions` 新增 `aiMode?: 'normal' | 'arena' | 'gauntlet' | 'corrupted' | 'boss'` 字段

---

## 四、AI 日志输出规范

AI 的每个决策应在战斗日志中输出，便于玩家理解和学习：

| 决策类型 | 日志格式                                           | 示例 |
| :------- | :------------------------------------------------- | :--- |
| 治疗     | `[敌方] 灼刃 使用 愈光 治疗 自身，回复 8 点生命`   | —    |
| 战吼     | `[敌方] 迅天 使用了 战吼，攻击力提升！`            | —    |
| 换位     | `[敌方] 棘尾 退到后排！`                           | —    |
| 攻击     | `[敌方] 灼天 对 迅迅 使用 火焰斩击，造成 8 点伤害` | —    |
| 嘲讽     | `[敌方] 棘尾 对 灼灼 施加 嘲讽`                    | —    |
| 观望     | `[敌方] 泡泡 无可用技能，在观望…`                  | —    |

> 注：当前 `useSkillInner` 已自动生成技能日志，AI 额外日志仅在特殊决策时输出（如换位、观望）。

---

## 五、AI 行为决策树完整版

```
┌─────────────────────────────────────────────────────────────────┐
│                    完整 AI 决策树                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  每个敌方单位行动时：                                          │
│                                                                 │
│  ① 是否眩晕？                                                  │
│     ├── 是 → 输出「被眩晕」日志 → 结束行动                    │
│     └── 否 → 进入 ②                                           │
│                                                                 │
│  ② 是否有治疗技能 且 治疗次数 > 0？                            │
│     ├── 是 → 检查血量阈值                                     │
│     │   ├── 自身血量 < 治疗阈值 → 治疗自己 → 结束行动         │
│     │   ├── 队友血量 < 治疗阈值-10% → 治疗队友 → 结束行动    │
│     │   └── 否 → 进入 ③                                      │
│     └── 否 → 进入 ③                                           │
│                                                                 │
│  ③ 是否有策略性技能（战吼/坚盾/嘲讽/群体控制）？              │
│     ├── 是 → 检查使用条件（概率判定）                         │
│     │   ├── 首回合 且 战吼可用 → 70% 概率使用战吼（特殊模式统一 70%）│
│     │   ├── 自身血量 < 40% 且 坚盾可用 → 50% 概率使用坚盾     │
│     │   ├── 敌方前排 ≥ 2 且 嘲讽可用 → 35%~60% 概率使用嘲讽   │
│     │   └── 否 → 进入 ④                                      │
│     └── 否 → 进入 ④                                           │
│                                                                 │
│  ④ 是否满足换位条件？                                          │
│     ├── 是（前排残血 + 健康后排）→ 40~65% 概率换位            │
│     └── 否 → 进入 ⑤                                           │
│                                                                 │
│  ⑤ 选择攻击技能（评分制）                                      │
│     ├── 有嘲讽状态 → 目标锁定嘲讽来源                         │
│     ├── 有状态配合被动 → 优先攻击带状态目标                   │
│     ├── 有残血目标（< 30%）→ 优先攻击                         │
│     ├── 有核心威胁（治疗/高输出）→ 优先攻击                   │
│     └── 以上都无 → 随机攻击                                   │
│                                                                 │
│  ⑥ 执行攻击 → 结束行动                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 六、风险提醒

1. **模拟测试**：每阶段改动后必须跑 `simulation.test.ts` 确保 20 种子中至少 1 局通关、0 卡死
2. **难度平衡**：AI 变强可能导致模拟胜率下降，可能需要同步调低敌人属性或提高玩家收益
3. **回合时长**：评分制比随机选择稍慢，但单次评分 < 0.1ms，影响可忽略
4. **不改动日志系统**：当前 `useSkillInner` 已自动生成技能日志，无需额外改动
