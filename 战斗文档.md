# 驯牌远征 · 战斗流程详解

> 依据 `src/game/core/battle.ts` 与 `src/game/data/` 代码整理，介绍从进入战斗到结算的完整流程、机制与数值，供玩家与设计参考。

---

## 目录

- [一、战斗触发与准备](#一战斗触发与准备)
- [二、战斗界面](#二战斗界面)
- [三、核心规则总览](#三核心规则总览)
- [四、一个回合的完整时序](#四一个回合的完整时序)
- [五、玩家阶段：可用操作](#五玩家阶段可用操作)
- [六、目标选择与前后排规则](#六目标选择与前后排规则)
- [七、技能结算流程](#七技能结算流程)
- [八、敌方 AI 逻辑](#八敌方-ai-逻辑)
- [九、状态效果与战斗药水](#九状态效果与战斗药水)
- [十、专属被动详解](#十专属被动详解)
- [十一、驯服机制](#十一驯服机制)
- [十二、胜负判定与战后结算](#十二胜负判定与战后结算)
- [十三、特殊战斗模式](#十三特殊战斗模式)
- [十四、常见问题与实战提示](#十四常见问题与实战提示)

---

## 一、战斗触发与准备

### 1.1 触发入口

玩家在地图上进入战斗类节点即触发战斗（普通遭遇战 / 精英 / 首领 / 斗兽场 / 车轮战 / 被侵蚀 / 守卫）。进入普通/精英/被侵蚀/守卫/**首领**节点时先进入**布阵界面**选择出战宠物与站位；斗兽场需**选择 1 只**出战；车轮战需先进入**出战顺序选择界面**，从全部宠物中选择 n 只（n v n，与敌方数量一致）并排出轮换上阵顺序。

### 1.2 出战与布阵

- 出战上限 **5 只**（`FIELD_MAX`）：前 3 只站**前排**（列 0~2），第 4 只起站**后排**（列 0~2）。战场共 6 格：前后排各 3 列。
- 布阵界面操作：左侧为**全部宠物池**（已上场宠物从列表消失），**拖拽**宠物到棋盘格子即上场（目标有宠物则交换、空位则移动），点击场上宠物可放回宠物池；最多出战 **5 只**（`FIELD_MAX`），但受敌方数量限制（敌方 n 只时我方最多 n+1 只）。
- **敌方数量固定**：由 encounter 列表决定，不再与我方出战数匹配（不再复制补齐）。**我方出战上限 = min(敌方数量+1, 5)**。
- 战斗开始时每个单位按速度计算行动顺序，并取得**行动点（AP）= 存活出战数**。

### 1.4 普通战斗敌人规模（概率表）

普通战斗的敌人规模按**幕数**和**行进度**（progress）决定，采用"数量×种类"定义（如 2/2 = 2 只、2 种不同物种）：

- **第一行**（progress=0）：强制 **1v1**（1 只）
- **第二行**（progress≈0.11）：**30% 1v1**、**70% 2v2**
- **其余行**按概率表：

| 幕数 | 2/2 | 3/3 | 4/4 |
|------|-----|-----|-----|
| Act 1 | 70% | 30% | — |
| Act 2 | 40% | 40% | 20% |
| Act 3 | 10% | 40% | 50% |

**行进度修正**（progress = 当前行 / (总行数-1)）：
- 前期（0.15 ≤ progress < 0.3）：+20% 2/2 权重，-10% 3/3，-10% 4/4（偏向小规模）
- 后期（progress > 0.7）：-20% 2/2，+5% 3/3，+15% 4/4（偏向大规模）

**种类规则**：从对应幕数的敌人池中随机选取不重复的物种（池足够大时自然不重复；池较小时允许少量重复）。

### 1.3 特殊模式（BattleOptions）

| 选项 | 效果 |
|---|---|
| `corruptDebuff: 'spd'` | 我方全体入场速度 -1（最低 1）——被侵蚀节点 |
| `corruptDebuff: 'dmg'` | 我方受到伤害 +1（每段受击都 +1）——被侵蚀节点 |
| `gauntlet: true` | 车轮战：敌我各一次只上一只，替补轮换上阵 |
| `untameable: true` | 敌方不可驯服（斗兽场/车轮战/守卫） |
| `enemyExact: true` | 敌方按指定数量**原样创建**：不按我方出战数压缩、不复制补齐（自定义测试用，可 1 挑 4） |

> 三种特殊模式的胜负与结算规则与普通战斗不同，见[第十三节](#十三特殊战斗模式)。

---

## 二、战斗界面

- **右侧**：竖向滚动日志面板（`.log-panel`），敌/我/系统分色显示。**战斗记录随动画逐条揭示**：新一回合结算的日志先不显示，每条日志在其对应攻击/治疗/掉血动画播放时（`useBattleFx` 按日志序号逐条推进 `revealedLogLen`）才出现，无动画的系统提示（info）与紧随其后的动画事件一并显示，动画全部播完后补全显示；面板最多展示最近 6 条。
- **攻击动画**：攻击/受击为纯视觉动画（攻击方冲刺、受击抖动+红闪、伤害/治疗数字飘字，灼烧/中毒持续伤害与反伤同样反馈），由 `src/ui/battleFx.ts` 解析战斗日志增量触发（按行动顺序排队播放，敌我一致），**不参与战斗逻辑**。日志条目携带 `actorUid`/`targetUid`（`LogEntry` 可选字段），动画**优先按 uid 精确定位单位**（同名敌人也能各自正确播放飘字/血条），缺失时才按名字反查兜底。**非攻击技能同样有施法动画**：治疗（如愈光）施法者金色发光施法 + 目标绿色治疗脉冲（+N 飘字）；buff 强化（如战吼）施法者发光 + 目标金色光环 + 状态飘字（按技能施加的状态显示，如「攻击↑」）。**每次命中都重启动画**（`fx` 带自增序号作为卡片 key 强制重挂载）：连击/随机多段（如叶针）逐段命中时，攻击者每次前冲、每个被命中目标每次抖动/红闪，飘字与血量条逐段推进；**若某一段后目标已阵亡（一段清场、敌方全灭），后续段不再命中、不产生攻击日志，动画随之停止**。日志每条附带当时全体血量快照（`LogEntry.hp`），**血量数字随动画事件逐个更新**（结束回合结算后先显示结算前血量，动画推进时逐步变化）；**胜负结算弹窗等动画全部播放完再显示**（我方全灭时 `animating` 期间失败界面不弹出，先播完死亡动画，最后一只宠物倒下后才显示失败界面）。**状态层数同样随动画事件回放**：日志每条附带当时全体状态快照（`LogEntry.statuses`，`pushLog` 统一写入、深拷贝），`useBattleFx` 按事件逐步推进显示——例如熔火领主「烈焰爆发」（技能灼烧 2 层 + 熔火灼烧被动 3 层）在攻击动画揭示时显示 **5 层**，其后同一结算里的回合开始 dot（-3）动画播放时同步降到剩 **2 层**，与血量条、飘字一致（不再从头到尾只显示结算后的 2 层）。状态图标同样与动画同步：**结算后新增的状态先隐藏**，待**附加它的那次攻击动画**播放时才揭示（攻击日志 `addsStatus` 标记本次攻击附加的状态 kind；灼烧/中毒/减防随该宠物攻击命中动画与扣血同步出现——**同一目标被多只宠物先后附加不同状态时，各自按攻击顺序依次揭示**；**连击等多段命中**时状态在全部段结算后才附加，故标记在最后一段、在最后一段攻击动画时揭示；**buff 状态（如战吼附加的 atkUp）随该单位施放 buff 技能的施法动画同步出现**——即使该单位本回合先受灼烧/中毒掉血（dot 动画在前）也不提前；其余匹配不到归属的状态才归入该单位作为目标的第一个事件兜底，目标从未出现才兜底第一个动画）；**回合结束灼烧/中毒层数归 0 而消失的状态先保持显示**，待其最后一次掉血动画播放时才移除（与飘字、血量条下降同步），无对应掉血事件的到期状态（如眩晕）随动画结束移除。新增状态检测在**渲染期**基于「上一快照 vs 当前快照」的差集计算（上一快照用 `useEffect` 同步到 ref，而非在 `useMemo` 工厂内更新，避免 StrictMode 双调用工厂时差集为空、状态在攻击动画前就显示）。
- **中部**：战场，前排 3 格 + 后排 3 格，显示双方宠物卡片（生命/速度/状态图标）；点击敌方单位弹出详情面板，展示其**被动技能与完整技能描述**（含剩余次数；不可驯服的对手不显示喂食按钮）。车轮战/单只出场时单位默认前排居中。
- **底部操作面板**（从左到右）：
  - **捕获区**：喂食驯服（选食物 → 点敌方；选中食物后按钮高亮、再次点击取消选中，把鼠标悬停到残血可驯服敌方上，会在其**宠物栏上覆盖显示**捕捉概率）；
  - **道具区**：食物 + 战斗药水；
  - **技能区**：当前选中我方单位的技能按钮（3 列网格，显示技能名 + 数值 + 描述 + 剩余次数）+ 「交换位置」「😴 休息」（本回合不出手，0 AP）。车轮战默认自动选中场上宠物，技能栏直接展示其技能（替补轮换后自动重选）；
  - **回合区**：⚡ 行动点（显示为「当前/上限」）+ 结束回合按钮。
- **提示栏**（`.hint-bar`，位于操作面板上方）：根据当前操作状态动态显示提示文字。选中技能等待选择目标时显示「⚡ [技能名]：请选择目标」；**技能指令已下达后**（与被动旁 `order-badge`「⚡ 已选择」出现时机一致）显示「⚡ [技能名]技能已选择」；选食物/道具/换位/确认出战等状态各有对应提示。

---

## 三、核心规则总览

- **属性**：只有 `最大生命（maxHp）` 与 `速度（spd）`，均为整数固定值；无攻击/防御/等级/经验。
- **伤害公式**：
  ```
  伤害 = 技能固定伤害 + 固定修正
  单段伤害 = max(1, 段基础伤害 - 减伤被动)（多段技能每段都扣减伤）
  ```
  每段技能伤害为固定值（多段技能逐段累计），最低 1 点；**减伤被动对多段攻击的每一段都生效**（与「受到的所有伤害 -N」描述一致）。固定修正见[第七节](#七技能结算流程)。
- **行动点（⚡ AP）**：每回合 AP = 我方存活出战数，界面显示为「当前/上限」。下达技能指令 / 换位各消耗 **1 AP**；喂食驯服、**休息**（本回合不行动）、**战斗药水**（不消耗 AP、不占用宠物行动，本回合宠物仍可出手）**不消耗 AP**；每只宠物每回合最多下达一条技能指令（重复点击技能 = 修改指令，**不重复扣 AP**；**再次点击已选技能 = 取消选择，退还 1 AP**）。
- **指令与结算**：玩家阶段为「**指令阶段**」——`playerSkill` 只给宠物**下达指令**（不立即结算），点「⚡ 执行指令」（`playerEndTurn`）后，敌我所有存活单位**按有效速度统一结算**：我方执行已下指令，敌方由 AI 自动行动；**未下指令的我方单位本回合不出手**。换位 / 喂食驯服仍即时生效；**战斗药水即时生效但不消耗 AP、不占用行动**（即使本回合全部宠物已行动、行动点耗尽，仍可补用药水，用后自动结束回合）；「**休息**」（`playerRest`）记入特殊指令 `orders[uid] = { skillId: 'rest' }`（**不消耗 AP**），结算时该单位跳过行动；休息与技能指令同属「已选择指令」，**可再次点击休息取消、或改点技能替换**（休息改技能需按新指令重新扣 1 AP）。
- **行动顺序**：结算时按**有效速度从高到低**依次行动（含药水 buff 修正；同速按 uid 排序）。
- **胜负**：敌方全灭即胜；我方全灭即败（斗兽场/车轮战例外）。

---

## 四、一个回合的完整时序

战斗为「回合制」，一回合 = **指令阶段 → 统一结算 → 回合开始**，如此循环：

```
┌─ 回合开始（startRound）
│   1. 战斗药水 buff 回合数递减（归 0 清除）
│   2. 重置行动标记：非眩晕单位可行动，眩晕单位跳过
│   3. 结算持续伤害（灼烧/中毒，按层数：掉 ceil(层数/2) 层并扣等量伤害）→ 死亡判定（**已死亡单位不再结算持续伤害**；灼烧/中毒中一个状态致死即停止剩余结算）
│   4. 其余回合制状态（战吼/铁刺/眩晕等）回合数 -1，归 0 移除
│   5. 触发「再生」类被动回血
│   6. 重新计算行动顺序；AP 恢复 = 存活出战数
│
├─ 指令阶段（playerAp > 0）
│   给任意存活宠物下达技能指令（每条约 1 AP）；换位/战斗药水/喂食驯服即时生效
│   点「⚡ 执行指令」（playerEndTurn）进入结算
│
├─ 统一结算（playerEndTurn）
│   所有存活单位（敌我混排）按有效速度从高到低依次行动：
│   我方执行已下达的技能指令；敌方由 AI 自动行动（治疗/换位/攻击）
│   未下指令的我方单位本回合不出手
│
└─ 回合结束 → 回到「回合开始」
```

- 玩家**手动执行指令**触发结算；指令可在结算前随时修改（不重复扣 AP）或**取消**（再次点击已选技能，退还 1 AP）。
- 敌方行动由 AI 自动完成，不可干预（详见[第八节](#八敌方-ai-逻辑)）。

### 首回合注意

战斗创建后立即进入「指令阶段」，**玩家先下指令**；随后统一结算、回合开始依次进行。

---

## 五、玩家阶段：可用操作

| 操作 | 消耗 | 说明 |
|---|---|---|
| **下达技能指令** | 1 AP | 点击己方单位选中 → 点击技能 → 选择目标（如需要）。不立即结算，记入该单位指令；重复点击技能可**修改指令**（不重复扣 AP）；**再次点击同一技能 = 取消选择**（`playerCancelOrder`，清空指令、恢复未行动、**退还 1 AP**）。有限次技能显示「剩 N 次」，耗尽禁用 |
| **休息** | 0 AP | 点击技能栏「😴 休息」按钮：记入特殊指令（`skillId: 'rest'`），与技能一样提示「⚡ 已选择」，本回合不出手；**再次点击取消选择**（恢复未行动状态），或改点技能替换（改技能按新指令扣 1 AP）。已下技能指令/眩晕/死亡/换位占用行动后禁用 |
| **交换位置** | 1 AP（发起者占用行动） | 己方任意两只互换位置（可前后/左右），**即时生效**，用于保护残血前排、调整站位 |
| **喂食驯服** | 0 AP | 选食物 → 点残血敌方（**即时生效**，不消耗 AP，见[第十一节](#十一驯服机制)） |
| **战斗药水** | 0 AP | 选药水 → 点目标（己方增益 / 敌方减益），**即时生效**；**不消耗 AP、不占用宠物行动**（本回合宠物仍可出手，行动点耗尽后也可补用） |
| **执行指令** | — | 结束指令阶段，敌我按速度统一结算 |

每只存活单位每回合最多下达一条技能指令（消耗 AP 并标记 `acted`）；换位 / 药水 / 驯服为即时行动，不写入指令（药水不消耗 AP 也不标记 `acted`）。

---

## 六、目标选择与前后排规则

技能通过 `target` 与 `reach` 决定能打谁：

| 目标类型 | 技能示例 | 规则 |
|---|---|---|
| 单体前排（默认 `front`） | 爪击、撕咬、水弹、火花 | 只能选**前排**存活敌人；前排全灭后可打后排 |
| 贯穿 `pierce` | 冲击波 | 命中前排，并**波及同列后排** |
| 后排 `back` | 狙击 | **跳过前排**直击后排（后排空则打前排） |
| 指定 `direct` | 穿刺 | **无视前后排**，指定任意位置 |
| 全体 `all` | 尾扫、烈焰爆发 | 攻击全部存活敌人 |
| 随机 `random` | 叶针（2 段） | 随机命中存活敌人（不重复，按段数；**目标唯一时所有段命中同一目标**） |
| 自身 `self` | 战吼 | 增益自身 |
| 队友 `ally` | 愈光 | 治疗指定队友（不指定默认治疗自己；**只能点己方单位，点敌方无效**） |

> 玩家在 UI 上可**自由点选目标**（符合规则即可）；敌方攻击目标由 AI 决定（多为随机/最低血量）。**嘲讽**：若敌方场上有被嘲讽的单位，敌方攻击技能强制以该单位为目标（无视正常目标选择规则）；被嘲讽的单位所有技能强制以嘲讽来源为目标（UI 只能选择嘲讽来源）；嘲讽持续 2 回合，到期自动消失。

---

## 七、技能结算流程

### 7.1 攻击技能

以技能为单位结算，对每个目标执行：

```
1. 基础伤害  base = 技能固定伤害 + 固定修正
2. 段数累计  每段伤害 = max(1, base)，多段技能逐段累计
3. 减伤修正  单段伤害 = max(1, 段基础伤害 - 受击者减伤被动)（多段逐段扣减伤）
4. 侵蚀修正  我方受击时被侵蚀「伤害加深」再 +1（多段每段 +1）
5. 扣除生命  受击者 hp -= 最终伤害（最低 0）；若受击者有护盾，护盾优先吸收伤害，护盾值不足时差值扣生命，护盾归 0 时移除护盾状态
6. 附加状态  技能自身效果（灼烧/中毒/降伤/眩晕）+ 攻击者 venom/scorch 被动
7. 被动连锁  攻击者 drain 吸血、受击者 thorns 反伤（多段每段命中都触发）
```

**多段命中（连击）**：连击（hits>1）对同一目标命中多次，减伤被动对**每一段**都生效（如 4×2 打铁壁 -1 → 每段 3、共 6；磐岩护甲 -3 打连击 → 每段保底 1、共 2）；**侵蚀「伤害加深」、drain 吸血、thorns 反伤同样按段生效**（连击 2 段各 +1、各吸一次、各反一次）。先按「单段基础伤害 - 减伤（每段 ≥1）+ 侵蚀」得到每段伤害，再乘以命中次数得总伤害，用 `splitDamage` 拆成 hits 段（总和不变、每段 ≥1，如 8 → 4+4），**逐段扣血、逐段写攻击日志、每段命中后紧跟吸血/反伤**，供动画逐段显示飘字；**若某一段已把目标击杀，后续段不再命中、不再写攻击日志**（一段清场时后续动画不播放）。**技能效果（灼烧/中毒/减防/眩晕）在每段攻击命中时都触发**（含击杀段），每段独立附加状态并逐段写入攻击日志的 `addsStatus`，供动画在该段播放时揭示状态标签。

**固定修正（`getDamageBonus`）来源**，全部叠加：

| 来源 | 修正 |
|---|---|
| 诅咒「虚弱」 | 伤害 -1 |
| 状态「战吼 atkUp」 | 伤害 + 状态值（战吼 +2） |
| 状态「铁刺 atkDown」 | 伤害 - 状态值（-1） |
| 状态「弱化 weaken（攻）」 | 伤害 - 状态值（随机减攻时 -1） |
| 攻击药水 | 伤害 +1 |
| 虚弱药水 | 伤害 -1 |
| 被动「蛮力/龙力/暗影之力」（power） | 伤害 + 被动值 |
| 被动「狂怒/炼狱狂暴/狂战」（frenzy） | 血量 <50% 时伤害 + 被动值 |

**减伤被动**（guard：铁壁/厚壳/磐岩护甲/磐岩之力）：受击时直接扣减 `被动值`（**多段攻击时每段各扣一次**）。

**攻击自愈**：部分攻击技能（如潮涌重击）附带 `heal` 字段，在所有目标结算后恢复自身生命（不超最大生命），产生独立的治疗日志。

### 7.2 治疗技能

- 愈光：治疗目标 **8 点**生命（不超最大生命），**每场限 2 次**。
- 水浴：恢复目标 **5 点**生命（不超最大生命），**每场限 2 次**。
- 敌方治疗同样按此结算，但受**每场 3 次上限**（`enemyHealsLeft`）约束；额度用尽后**拒绝治疗**（AI 不再选择治疗技能，直接调用也视为施展失败）。

### 7.3 增益 / 防御技能

- 战吼：自身获得「战吼 atkUp」状态（伤害 +2，持续 2 回合），**每场限 2 次**。
- 坚盾：为目标附加 **5 点护盾**（`shield`），**每场限 2 次**。护盾优先于生命值吸收伤害，护盾值归 0 时移除护盾状态。

### 7.4 技能次数

- `SkillDef.uses` 设置每场战斗可用次数（缺省 = 无限，`skillUsesLeft` 返回 Infinity）。
- 玩家与敌方使用有限次技能后各扣 1 次；耗尽后无法再使用，UI 按钮禁用并显示「剩 N 次 / 已用完」。
- 已设置限次技能：**愈光 2 次、战吼 2 次、噬魂斩 2 次、坚盾 2 次、水浴 2 次**。

---

## 八、敌方 AI 逻辑

敌方单位在自己的行动回合执行如下判断（`enemyAct`）：

```
1. 眩晕？→ 记录「被眩晕，无法行动」日志并跳过行动
2. 过滤可用技能（次数耗尽的剔除；无可用 → 观望）
3. 治疗判定：有治疗技能 且 敌方治疗次数 > 0
   且 自身血量 < 50% 且 随机数 > 0.3
   → 治疗场上生命比例最低的队友（含自己）
4. 换位判定：随机数 < 0.15 且 自身是残血前排（<50%）且有血量健康的后排
   → 退到后排（占用行动）
5. 否则：随机选择一个可用攻击技能；若治疗次数已用尽，则先从可用技能中剔除治疗技能
```

- 敌方每场战斗治疗**最多 3 次**，防止治疗无限拉长战斗；次数用尽后 AI 不再选择治疗技能。
- 敌方调整站位只能通过「残血前排退到后排」（与血量健康的后排交换位置），不能主动把后排补到前排。

---

## 九、状态效果与战斗药水

### 9.1 持续状态

| 状态 | 效果 | 持续 |
|---|---|---|
| 灼烧 burn | 每回合掉 ceil(层数/2) 层（向上进位）并扣等量伤害 | 层数，可叠加 |
| 中毒 poison | 每回合掉 ceil(层数/2) 层（向上进位）并扣等量伤害 | 层数，可叠加 |
| 战吼 atkUp | 伤害 +2 | 2 回合 |
| 铁刺 atkDown | 伤害 -1 | 2 回合 |
| 眩晕 stun | 跳过行动 | 回合数递减（当前无技能直接附加） |
| 护盾 shield | 吸收伤害优先于生命值 | 持续至耗尽或被移除 |
| 嘲讽 taunt | 敌方攻击时强制以嘲讽单位为目标，被嘲讽者所有技能只能攻击嘲讽来源 | 2 回合 |
| 弱化 weaken | 随机降低目标攻击或速度 2 层 | 2 回合 |

- 持续伤害在**回合开始时结算**（先于行动顺序）：灼烧/中毒**按层数**掉 `ceil(层数/2)` 层（向上进位，如 3 层掉 2）并扣等量伤害、消耗等量层数，归 0 移除；其余回合制状态回合数 -1。
- 状态附加时**灼烧/中毒可叠加（层数相加）**，其余同类状态取**较大值/较长持续**。

### 9.2 战斗药水（buff / debuff）

在玩家行动回合使用，**消耗 1 AP**：

| 药水 | 目标 | 效果 | 持续 |
|---|---|---|---|
| ⚔️ 攻击药水 | 我方单体 | 伤害 +1 | 3 回合 |
| 💨 速度药水 | 我方单体 | 速度 +1 | 3 回合 |
| 💚 生命药水 | 我方单体 | 回复 50% 最大生命 | 即时 |
| 🪄 虚弱药水 | 敌方单体 | 伤害 -1 | 3 回合 |
| 🕸️ 缓速药水 | 敌方单体 | 速度 -1 | 3 回合 |
| ☠️ 腐蚀药水 | 敌方单体 | 当前生命 -30%（至少保留 1 血）；对 boss 改为造成已损失生命 30% 伤害（至少 5 点） | 即时 |

- 药水 buff 在**每回合开始递减**，持续满 3 回合后消失。
- 伤害修正进 `getDamageBonus`，速度修正进 `getEffectiveSpd`（影响行动顺序，最低 1）。

---

## 十、专属被动详解

每只生物（含敌方/Boss/造物）各带 1 个被动，战斗内自动生效：

| 类型 | 效果 | 触发时机 |
|---|---|---|
| hp | 最大生命 +N | 入场 |
| spd | 入场速度 +N | 入场 |
| regen（水愈/深海再生/古树再生） | 每回合开始恢复生命 | 回合开始 |
| guard（铁壁/厚壳/磐岩护甲） | 受到的所有伤害 -N | 受击结算 |
| thorns（尖刺/硬甲/玄铁反甲） | 受到攻击时反伤 N（多段每次命中都触发） | 受击后 |
| drain（潮汐吸噬/迅风） | 造成伤害后恢复 N 生命（多段每次命中都触发），产生独立回血日志 | 攻击结算 |
| venom / scorch（毒牙/毒尾/剧毒孢子 / 炽热/烈焰/熔火灼烧） | 攻击命中附加中毒 / 灼烧 N | 攻击命中 |
| power（蛮力/龙力/暗影之力） | 所有技能伤害 +N | 伤害计算 |
| frenzy（狂怒/炼狱狂暴/狂战） | 血量 <50% 时伤害 +N | 伤害计算 |

> 完整列表见 [`生物图鉴.md`](生物图鉴.md)。被动叠加伤害修正时与状态/诅咒/药水共同计入（见 §7.1）。

---

## 十一、驯服机制

在战斗中把敌方打残后可喂食驯服。

**条件**（全部满足）：
1. 该敌人**可驯服**：品阶 `rank < 4`（首领 rank 4 不可驯服），且不属于不可捕捉类型（斗兽场/车轮战/守卫对手）；
2. 敌人当前生命 **≤ 40%** 最大生命；
3. 拥有对应食物。

**成功率**：
```
成功率 = 食物基础率 × 物种驯服难度 × 生命因子 × (1 + 失败次数 × 25%)
生命因子 = 0.4 + 0.6 × (1 - 当前生命 / 最大生命)
```

- 血越低成功率越高（40% 时因子 0.4，濒死时接近 1.0）。
- **每次驯服失败**，该敌人后续捕捉概率 +25%（乘法累加）。
- **生命 1 点时必定捕捉**（仍要求可驯服）；**圣果**使用必定成功（也仍要求残血且可驯服）。
- 每次喂食消耗 1 个食物，**不消耗 AP**。

**食物数据**：

| 食物 | 基础成功率 | 生命加成 | 价格 |
|---|---|---|---|
| 🍓 浆果 | 70% | +1 | 5 |
| 🍖 鲜肉 | 85% | 0 | 9 |
| 💎 秘晶 | 95% | +3 | 14 |
| 🍎 圣果 | 100%（必定） | +5 | 不售卖 |

**驯服成功**：敌人离场加入「队伍预备役」，战后正式入队（继承食物生命加成）；队伍满 8 只时进入「处理队伍」界面（替换/融合/放生）。

---

## 十二、胜负判定与战后结算

### 12.1 胜负判定

| 结果 | 条件 | 后续 |
|---|---|---|
| 胜利 | 敌方全部退场（含车轮战替补耗尽） | 战后结算 |
| 失败 | 我方全部退场（普通/精英/首领/被侵蚀） | Game Over（宠物永久删除） |
| 失败 | 斗兽场/车轮战 | 坏事件二选一惩罚，宠物不死（保底 1 血） |

### 12.2 战后结算（胜利）

1. **同步存活**：战斗中存活单位数据回写队伍；**普通战斗阵亡宠物永久删除**（斗兽场/车轮战阵亡保底 1 血保留）。
2. **驯服入库**：预备役宠物入队（满则溢出）。
3. **金币**：战斗/精英/守卫 **16**、首领 **30**；被侵蚀（金币型）**32**；斗兽场/车轮战无固定金币。
4. **守卫**：额外获得对应**专用钥匙**。
5. **全体回血 50%**。
6. **奖励二选一**：随机食物 1 个 / 全体回血 30% / 招募 1 只随机普通宠物（队伍未满时出现）。第 1 行（出发）奖励必含招募。斗兽场/车轮战改为**挑战奖励 3 选 1**（30 金币 / 随机食物 / 回血 50% / 招募）。

> 被侵蚀（食物型）：食物奖励翻倍为 2 个。

---

## 十三、特殊战斗模式

### 13.1 斗兽场 🗡️（1v1）

- 选 **1 只**出战，与 1 只精英池怪单挑（幕 1：棘尾/铁卫；幕 2~3：迅天）。
- 敌方**不可驯服**。
- 胜利：挑战奖励 3 选 1（无固定金币）；失败：坏事件二选一惩罚（损失 15~25% 生命 或 20~35 金币），宠物不阵亡。

### 13.2 车轮战 🔥（轮换上阵）

- 进入节点后先进入**出战顺序选择界面**：从全部宠物中选择 n 只出战（n v n，与敌方数量一致），可拖拽排序（左侧槽位拖动可交换、点击槽位宠物放回宠物池、点击宠物池宠物填入空槽）。
- 敌方 2~3 只**轮换上阵**：场上同时只存在 1 只，倒下后下一只满血登场；**刚切入的敌方替补当回合不出手**（`acted=true`，回合结算跳过），下回合才正常行动。
- **我方也一次只上一只**：其余进入替补席，场上阵亡后**等死亡动画播完自动按序顶替**（阵亡单位不复活、退出战场显示；仅 1 只时默认前排居中）。动画播完前结算面板暂停（不弹胜负、不结束回合），由 `GAUNTLET_SWAP` 触发换人并写「派出下一只」日志。
- 战斗中**默认自动选中上场宠物**，技能栏直接展示其技能；替补轮换上场后自动重选新单位。
- 敌方不可驯服；胜利/失败规则同斗兽场。

### 13.3 被侵蚀 🌑（奖励翻倍）

- 普通战斗的变形，带随机暗影 debuff（入场固定，可复现）：
  - `spd`：我方全体速度 -1；
  - `dmg`：我方受到伤害 +1。
- 胜利后奖励翻倍：金币型 16 → **32**；食物型食物 ×2。
- 失败与普通战斗一致（Game Over）。

> 三种特殊节点均可用**跳关道具**跳过直接结算奖励（挑战节点为 3 选 1）；首领与守卫不可跳。

---

## 十四、常见问题与实战提示

**Q：出战只有 2 只宠物时会怎样？**
敌方也以 2 只参战，保证公平；我方少于 2 只（1 只）时敌方 1 只。

**Q：前排敌人没清完能打后排吗？**
默认单攻技能只能打前排；用「狙击」（跳排）、「穿刺」（指定）、「冲击波」（贯穿波及后排）或群攻技能可突破前排。

**Q：为什么敌方奶妈一场战斗只奶了几次就不奶了？**
敌方治疗每场限 3 次，且要求自身血量 <50% 才优先治疗；同时受技能次数（愈光 2 次）约束。

**Q：攻击药水加了伤害，但显示数值没变？**
buff 在伤害结算时生效（`getDamageBonus`），与战吼/被动叠加；速度药水影响的是行动顺序而非面板速度。

**实战提示**：
- 战吼 + 攻击药水 + 蛮力被动可让单段伤害显著提升，配合多段技能（连击/叶针）收益更高。
- 速度药水给坦克或主力抢先手；缓速药水给高速敌人争取先手权。
- 残血前排被集火时，用**换位**把它换到后排保命。
- 喂食驯服前先看悬停概率：血量越低越稳，1 血必成。
- 眩晕敌人会跳过行动（结算时产生「被眩晕，无法行动」日志），但玩家可以攻击眩晕单位；眩晕期间它不会反击。

---

> 相关文档：[`游戏玩法说明.md`](游戏玩法说明.md) ｜ [`关卡图鉴.md`](关卡图鉴.md) ｜ [`生物图鉴.md`](生物图鉴.md) ｜ [`道具图鉴.md`](道具图鉴.md)
